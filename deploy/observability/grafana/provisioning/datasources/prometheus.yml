apiVersion: 1
datasources:
  # The provisioned dashboard refers to this UID, so it must stay stable.
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
